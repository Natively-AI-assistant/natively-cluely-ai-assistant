export function computeStealthCaretLeft(params: {
  textWidth: number;
  paddingLeft: number;
  contentWidth: number;
}): number;

export function measureTextWidth(text: string, font: string): number;
